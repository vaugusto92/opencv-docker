on:
  push

name: ci

jobs:
  build:
    name: Build image
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v3
      -
        name: Run docker build   
        run: |
          set -x
          docker version
          docker build -t opencv .
